package unittests;

import geometries.Cylinder;
import org.junit.Test;
import primitives.*;

import static org.junit.Assert.*;
/**
 * @author 
 *Unit tests for primitives.Cylinder class
 */

public class CylinderTests {

    /**
     * Test method for normal to cylinder.
     */
    //@Test
    //public void getNormal()
    //{
    //    Cylinder t = new Cylinder(1,new Ray(new Point3D(1,0,0), new Vector(0,1,0)), 1);
    //    assertEquals("Wrong normal to Cylinder", new Vector(0, 1, 0), t.getNormal(new Point3D(2, 0, 0)));
    //}
	/**
     * Test method for intersection Points with ray and cylinder
     */
	public void testFindIntersectionPoints()
	{
		
	}
}
